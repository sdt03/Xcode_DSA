#include<iostream>
#include<vector>

using namespace std;

long long subArrayRanges(vector<int>& arr){
    long long sum = 0;
    
    
    
    
    return sum;
}

int main(){
    vector<int> arr = {4,-2,-3,4,1};
    long long sum = subArrayRanges(arr);
    cout << sum << " ";
    return 0;
}
